[project]
name = "lidar-fuel-ml"
version = "0.1.0"
description = "LiDAR-derived fuel and canopy base height modeling pipeline"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "laspy>=2.5",
    "lazrs>=0.6",
    "numpy>=2.1",
    "scikit-learn>=1.6",
    "scipy>=1.15",
]
